describe ('weather', function () {
  var weather;

  beforeEach(function(){
    weather = new Weather();
  });

  // it('should be sunny all the time (not)', function() {
  //   expect(weather.currentWeather()).toEqual("sunny");
  // });
});
